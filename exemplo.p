program exemplo; {programa exemplo}
    var a, b: integer;

    procedure p( x : integer; y : boolean );
        var b, c: integer;
        begin
            b := 5 + 3;
    end;
    procedure q;
        var c, d: integer;

        procedure r( y : integer );
            var e, f: integer;
            begin
            e := 5;
        end;

        begin

    end;

    begin

end.