program exemplo; {programa exemplo}
    var a, b: integer;

    procedure p( x );
        var b, c: integer;
        begin
            d := 5;
    end
    procedure q;
        var c, d: integer;

        procedure r( y );
            var e, f: integer;
            begin
            g -> 5;
        end

        begin

    end

    begin

end.